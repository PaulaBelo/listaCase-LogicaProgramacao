var retangulo = function(){
    let altura = (10);
    let base = (20);
    let diagonal = Math.sqrt(base*base + altura*altura);
    console.log  ('O perimetro é:'+ 2*(base+altura));
    console.log  ('A area é:'+ base*altura);
    console.log ('A diagonal é:'+ diagonal)
  }
  retangulo()